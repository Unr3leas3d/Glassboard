{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sessionbar",
  "description": "Minimal capabilities for the deprecated session bar window",
  "windows": ["sessionbar"],
  "permissions": [
    "core:default",
    "core:window:allow-set-focus",
    "core:event:default"
  ]
}
